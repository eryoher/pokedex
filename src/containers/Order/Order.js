// @flow
import React, { Component } from 'react';
import { Jumbotron, Row, Col, Image, Button } from 'reactstrap';
import { Grid } from 'react-bootstrap';

type Props = {
  theme: Object
};

export default class Order extends Component<Props> {
  render() {
    return (
      <div>
        PEDIDO PAGE
      </div>
    )
  }
}
